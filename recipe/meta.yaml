{% set name = "Unidecode" %}
{% set version = "1.0.22" %}
{% set sha256 = "8c33dd588e0c9bc22a76eaa0c715a5434851f726131bd44a6c26471746efabf5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 1
  script: python -m pip install --no-deps --ignore-installed .
  entry_points:
    - unidecode = unidecode.util:main

requirements:
  host:
    - pip
    - python
    - setuptools
  run:
    - python

test:
  imports:
    - unidecode
  commands:
    - unidecode --help

about:
  home: https://pypi.python.org/pypi/Unidecode
  license: GPL-2.0
  license_file: LICENSE
  summary: 'ASCII transliterations of Unicode text'
  dev_url: https://www.tablix.org/~avian/git/unidecode.git

extra:
  recipe-maintainers:
    - synapticarbors
